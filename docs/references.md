# References

Example repos cloned locally for pattern lookup while developing. Not part of this build.

```bash
pnpm references:setup                    # ../references + multi-root workspace
git clone <url> ../references/<name>
```

Open `../new-thinkex.code-workspace`. Excluded from lint/typecheck — borrow ideas, don't import from here.
